<?xml version="1.0" encoding="UTF-8"?>
<odoo>
	<data>

	<!-- Rename Lock to Close -->
	<record id="purchase_order_lock_to_close_state_form" model="ir.ui.view">
            <field name="name">purchase.order.lock.to.close.form</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
            	<button name="button_done" position="replace">
            		<button name="button_done" type="object" string="Close" states="purchase"/>
            	</button>
            </field>
        </record>

	</data>
</odoo>